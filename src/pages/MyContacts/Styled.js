import styled from 'styled-components';

export const StyledContactsList = styled.div`
  overflow: scroll;

  .main-title {
    color: #000;
    font-size: 18px;
    font-style: normal;
    font-weight: 300;
    line-height: normal;
  }
  .contacts-list {
    padding: 0px;
  }
`;
